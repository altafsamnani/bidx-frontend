<?php
/* Default Theme Variables */
/* 1 Alignment Settings Variables */
if ( !defined( 'BIDX_PAGE_WIDTH_SELECTOR' ) ) {
  define( 'BIDX_PAGE_WIDTH_SELECTOR', 'full' );
}

if ( !defined( 'BIDX_LOGO_ALIGNMENT' ) ) {
  define( 'BIDX_LOGO_ALIGNMENT', 'left' );
}

if ( !defined( 'BIDX_LOGO_ALIGNMENT' ) ) {
  define( 'BIDX_LOGO_ALIGNMENT', 'left' );
}

if ( !defined( 'BIDX_SIDEBAR_ALIGNMENT' ) ) {
  define( 'BIDX_SIDEBAR_ALIGNMENT', 'right' );
}

/* 2 Font Settings Variables */
if ( !defined( 'BIDX_TEXT_FONT') ) {
  define( 'BIDX_TEXT_FONT', 'Lato' );
}

if ( !defined( 'BIDX_HEADINGS_FONT') ) {
  define( 'BIDX_HEADINGS_FONT', 'PT Sans' );
}

if ( !defined( 'BIDX_MENU_FONT') ) {
  define( 'BIDX_MENU_FONT', 'Ubuntu Condensed' );
}

if ( !defined( 'BIDX_FONT_SIZE') ) {
  define( 'BIDX_FONT_SIZE', 'medium' );
}

/* 3 Color Settings Variables */
if ( !defined( 'BIDX_BRAND_PRIMARY') ) {
  define( 'BIDX_BRAND_PRIMARY', '#3498db' );
}
if ( !defined( 'BIDX_BRAND_SECONDARY') ) {
  define( 'BIDX_BRAND_SECONDARY', '#1abc9c' );
}
if ( !defined( 'BIDX_BACKGROUND_COLOR') ) {
  define( 'BIDX_BACKGROUND_COLOR', '#FFF' );
}
if ( !defined( 'BIDX_BRAND_FULL_PATTERN') ) {
  define( 'BIDX_BRAND_FULL_PATTERN', 'full' );
}

?>